"""Command-line entry point, including the benchmark runner."""


def main() -> None:
    """Placeholder entry point; the real CLI arrives with the solver."""
    print(f"tessera {__import__('tessera').__version__}")
