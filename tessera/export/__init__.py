"""Rendering timetables out: PDF, self-contained HTML, CSV, calendar subscriptions.

Generated here rather than in the macOS client so that every deployment can export,
and so the output is testable in CI without a Mac (ADR-007).
"""
