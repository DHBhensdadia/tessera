"""Tessera — university timetable scheduling.

The engine is the whole product. It ships bundled inside the macOS app, as a Docker
image, and as a command-line tool, from this one package.
"""

__version__ = "0.4.0"
